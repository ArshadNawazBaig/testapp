import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const Wrapper = styled(Box)`
  width: 50%;
  margin: auto;
`;

export const MainHeadingWrapper = styled(Box)`
  margin: 48px 0;
`;
export const SubHeadingWrapper = styled(Box)`
  margin: 48px 0 16px 0;
`;
